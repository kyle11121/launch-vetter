{
  "name": "launch-vetter",
  "version": "1.0.0",
  "description": "Pivotree product launch vetter",
  "type": "module",
  "main": "server.js",
  "scripts": {
    "build": "cd client && npm install && npm run build",
    "start": "node server.js",
    "dev": "node server.js"
  },
  "dependencies": {
    "express": "^4.18.2",
    "cors": "^2.8.5",
    "docx": "^8.5.0"
  }
}
